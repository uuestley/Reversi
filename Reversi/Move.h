#ifndef MOVE_HEADER
#define MOVE_HEADER

// A type definition of a single unsigned integer which
// represents the index of the result of the move (where
// the piece is moved to)
typedef unsigned int Move;

#endif